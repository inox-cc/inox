# node:dns/promises

Promise entrypoint for the implemented `node:dns` operating-system lookup
APIs. It exposes `lookup()` including `{ all, order }` options and
`lookupService()` with the same behavior as `dns.promises`.

See the parent [`node:dns`](../README.md) package for backend requirements and
known limitations.
