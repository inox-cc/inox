{
  "name": "ccjs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "bin": {
    "ccjs": "./bin/ccjs.ts"
  },
  "scripts": {
    "build": "node scripts/build.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "test:fixtures": "node scripts/test-fixtures.ts",
    "test:ir-snapshots": "node scripts/test-ir-snapshots.ts",
    "test:codegen-snapshots": "node scripts/test-codegen-snapshots.ts",
    "test:snapshots": "npm run test:ir-snapshots && npm run test:codegen-snapshots",
    "check": "npm run build && npm run typecheck && npm test && npm run test:fixtures && npm run test:snapshots"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "typescript": "^6.0.3"
  }
}
